# 패킷 미전송

## 관련 키워드
패킷, 전송 실패, 미전송, 데이터 누락, SEND_TXN_DATA_DIRECTLY, txn spec out, 사용자 전문, xHeaderInfo, UDP, 분기 누락

## 과거 사례
| 이슈 | 고객사 | 컴포넌트 | 버전 | 근본 원인 요약 | 핵심 파일 |
|------|--------|----------|------|--------------|----------|
| IMX-9604 | 공통 | JSPD | 250904 | `xHeaderInfoSecure()`에 `SEND_TXN_DATA_DIRECTLY` 분기 누락으로 txn spec out 모드에서 암호화 패킷 미전송 | com/exem/jspd/udp/e.java:325 |

## 공통 패턴

## 조사 시 체크포인트
1. `SEND_TXN_DATA_DIRECTLY` 설정값 확인 (true=txn spec out 모드)
2. 전송 메소드별 분기 처리 일관성 확인 (일반 메소드에는 있고 변형 메소드에는 누락되는 패턴)
3. UDP 큐 방식(`this.new()`) vs 직접 전송 경로가 모두 커버되는지 확인
4. XAPM_USER_DATA 테이블에 데이터 적재 여부로 전송 성공 확인
