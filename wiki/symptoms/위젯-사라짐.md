# 위젯 사라짐/깜빡임

## 관련 키워드
위젯 사라짐, 카드 사라짐, 깜빡임, 통합대시보드, 그룹 카드, disable 토글, 연계 제품 연결 실패, 빈 리스트, 레이아웃 유실

## 과거 사례
| 이슈 | 고객사 | 컴포넌트 | 버전 | 근본 원인 요약 | 핵심 파일 |
|------|--------|----------|------|--------------|----------|
| IMX-9237 | HL만도 | PeakVisor | v1.0.0.5-alpha.0 | 연계 제품 API 실패 시 캐시 빈 리스트 전파 + disable 상태 빠른 토글로 카드 레이아웃 자체 유실 | ApiProcessor.java:74-98, LinkInfoProvider.java:31-69, CacheScheduler.java:58-108 |

## 공통 패턴

## 조사 시 체크포인트
1. 연계 제품 서버(MFS, APM 등)의 네트워크 연결 상태 확인
2. 로그에서 `LinkInfoProvider`의 `currentDisables` 값 토글 빈도 확인
3. `ResourceAccessException` / `ConnectException` 에러 로그 확인
4. CacheScheduler의 API 호출 간격과 동일 origin 대상 다중 API 호출 패턴 확인
5. Caffeine 캐시 TTL 만료 시 빈 리스트로 전파되는지 확인
6. 프론트엔드에서 빈 target 리스트일 때 카드 레이아웃 유지 여부 확인
