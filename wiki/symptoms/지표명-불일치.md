# 지표명 불일치

## 관련 키워드
알람명, 지표명, .NET, JVM, DOTNET, alertResourceName, ALERT_RESOURCE_NAME, 알람 설정, 알람 발생 내역, 이름 변환, 라벨 불일치

## 과거 사례
| 이슈 | 고객사 | 컴포넌트 | 버전 | 근본 원인 요약 | 핵심 파일 |
|------|--------|----------|------|--------------|----------|
| IMX-7631 | 공통 | DataGather, PlatformJS | v5.4.9.1-alpha.2 | DB에 JVM 이름으로 통일 저장하는 설계이나, 알람 발생 내역 등 5개 화면에서 .NET 변환 로직 미구현 | AlertHistory.js:1442, rtmAlertInfo.js:355, rtmAlertLight.js:603, config_alarmMemoHistory.js:235, config_alarmStopHistory.js:286 |

## 공통 패턴
{2건 이상 사례가 쌓이면 작성}

## 조사 시 체크포인트
1. DB 테이블(XAPM_ALERT_CONFIG)의 ALERT_RESOURCE_NAME 칼럼에 저장된 이름 확인
2. 프론트엔드에서 서버 타입(DOTNET/JVM) 분기 처리 여부 확인
3. jvmStatTransObj 변환 매핑이 해당 화면에 적용되어 있는지 확인
4. 알람 설정 UI와 알람 조회 UI 간 이름 표시 일관성 확인
